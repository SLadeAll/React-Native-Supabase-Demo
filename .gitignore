.vercel

# Local secrets only — .env.example is intentionally tracked as a template.
.env
.env.local
.env.*
!.env.example

# dependencies
node_modules/

# editor/tooling local state
.history/

# Supabase CLI local runtime state (not project config)
supabase/.branches/
supabase/.temp/
